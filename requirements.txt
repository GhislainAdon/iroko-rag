litellm==1.84.0
# openai-agents  # optional: required for examples/agentic_vectorless_rag_demo.py
pymupdf==1.26.4
PyPDF2==3.0.1
python-dotenv==1.2.2
pyyaml==6.0.2

# Optional deps for ingest.py (universal ingestion):
# markitdown    # pptx/xlsx/csv/... -> Markdown
# pytesseract   # OCR fallback for scanned PDFs (needs tesseract on PATH)
# pillow        # OCR fallback image handling
# ocrmypdf      # better OCR path, preserves page numbers
